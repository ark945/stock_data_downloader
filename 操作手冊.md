# 📥 台股全市場券商分點資料下載與爬蟲系統 — 操作手冊 (User Manual)

---

## 📌 目錄
1. [系統簡介與核心功能](#1-系統簡介與核心功能)
2. [專案目錄架構與模組清單](#2-專案目錄架構與模組清單)
3. [環境建置與一鍵設定精靈](#3-環境建置與一鍵設定精靈)
4. [組態設定說明 (Config & Settings)](#4-組態設定說明-config--settings)
5. [操作指令與使用範例](#5-操作指令與使用範例)
   - [5.1 互動式設定與一鍵診斷精靈 (setup_wizard.py - 最強烈推薦)](#51-互動式設定與一鍵診斷精靈-setup_wizardpy---最強烈推薦)
   - [5.2 一鍵下載全市場（上市 + 上櫃）分點日報表](#52-一鍵下載全市場上市--上櫃分點日報表)
   - [5.3 僅下載上市股票 (TWSE)](#53-僅下載上市股票-twse)
   - [5.4 僅下載上櫃股票 (TPEX)](#54-僅下載上櫃股票-tpex)
   - [5.5 雲端分散式矩陣分片執行與二次聚合架構 (Matrix Sharding & Merge)](#55-雲端分散式矩陣分片執行與二次聚合架構-matrix-sharding--merge)
   - [5.6 驗證與自動補抓遺漏標的 (Verify & Repair)](#56-驗證與自動補抓遺漏標的-verify--repair)
   - [5.7 Parquet 轉 Excel 工具 (parquet_to_excel.py)](#57-parquet-轉-excel-工具-parquet_to_excelpy)
6. [TWSE 雙引擎驗證碼辨識機制解析 (CNN vs ddddocr)](#6-twse-雙引擎驗證碼辨識機制解析-cnn-vs-ddddocr)
7. [TPEX 上櫃雙環境拆分架構與對策 (Cloud vs Local)](#7-tpex-上櫃雙環境拆分架構與對策-cloud-vs-local)
8. [輸出結果與資料結構規格](#8-輸出結果與資料結構規格)
   - [8.1 Parquet 資料庫命名規範 (全市場 / 上市 / 上櫃)](#81-parquet-資料庫命名規範-全市場--上市--上櫃)
   - [8.2 分片日誌 (Log) 彙整與 Google Drive Log 資料夾歸檔](#82-分片日誌-log-彙整與-google-drive-log-資料夾歸檔)
   - [8.3 標準 13 欄位定義](#83-標準-13-欄位定義)
9. [雲端排程與 Secrets 密鑰申請指南](#9-雲端排程與-secrets-密鑰申請指南)
10. [常見問題排解 (FAQ & Troubleshooting)](#10-常見問題排解-faq--troubleshooting)

---

## 1. 系統簡介與核心功能

本系統專門負責台灣證券市場（上市 TWSE 與上櫃 TPEX）全市場個股「券商買賣日報表」之全自動化抓取、驗證碼辨識、資料清洗、分片聚合與標準化儲存。

### 🌟 核心特色
1. **雙市場全覆蓋與二次聚合**：
   - 雲端排程自動產出上市 `api_absr1_YYYY-MM-DD_YYYY-MM-DD_twse.parquet`、上櫃 `api_absr1_YYYY-MM-DD_YYYY-MM-DD_tpex.parquet`，並自動執行二次聚合輸出全市場統一之 `api_absr1_YYYY-MM-DD_YYYY-MM-DD.parquet`。
2. **分片日誌全自動彙整歸檔**：
   - 自動將 TWSE（6 分片）與 TPEX（20 分片）的所有執行 Log 按 Shard 編號排序彙整為 `YYYY-MM-DD-twse.log` 與 `YYYY-MM-DD-tpex.log`，並自動同步上傳至 Google Drive 的 `Log/` 子目錄。
3. **驗證碼雙引擎破解**：
   - 優先級 1：專用 CNN 深度學習模型（辨識率 98%+）
   - 優先級 2：通用 `ddddocr` 模型備援（辨識率 88%~92%）
4. **TPEX 雙環境拆分架構**：
   - ☁️ **雲端環境 (`tpex_crawler_cloud.py`)**：20 節點海外矩陣分片、單會話純淨防禦，徹底規避 Cloudflare 限流。
   - 💻 **本地環境 (`tpex_crawler_local.py`)**：4~8 Workers 多進程平行矩陣 + **斷點續傳快取 (Checkpoint Resume)**，提速 600%，中斷秒級接關！
5. **最多 6 輪自適應安全補抓 (Adaptive Retry)**：若遇驗證碼或網路波動，系統自動在後續輪次進行自適應降速補抓，保證 100% 達成率。
6. **多管道推播與雲端同步**：支援 Google Drive（GAS 模式與 Service Account 雙模式）、Telegram 即時通知與 Gmail HTML 郵件日報。
7. **標準化數據規格**：自動輸出統一 13 欄位格式，並以高壓縮 Meta ZSTD Parquet 格式與 Excel (.xlsx) 儲存。

---

## 2. 專案目錄架構與模組清單

```
stock_data_downloader/
├── stock_crawler_coordinator.py    # [核心入口] 全市場台股日報表統一調度器 (支援分片與多線程)
├── twse_bsr_crawler.py             # 上市 (TWSE) 專用高速爬蟲 (雙引擎 + 6 輪自適應補抓)
├── tpex_bsr_crawler.py             # 上櫃 (TPEX) 統一轉發閘道 (自適應環境分流)
├── tpex_crawler_local.py           # [本地專用] TPEX 多進程極速並行引擎 (CDP 封包監聽 + 斷點續傳)
├── tpex_crawler_cloud.py           # [雲端專用] TPEX 20-Shard 單會話穩健防禦引擎 (CDP 封包監聽)
├── check_gdrive_exists.py          # [雲端檢查] 3 秒 Google Drive 存在性極速快篩模組 (備援排程智能跳過)
├── gdrive_sync.py                  # Google Drive 雲端同步模組 (支援 GAS / SA 雙架構與 Log 子資料夾自動歸檔)
├── merge_shards.py                 # 分片成果聚合器 (支援 Parquet 二次聚合與 Log 彙整上傳)
├── captcha_engine.py               # TWSE 驗證碼辨識模組 (CNN + ddddocr)
├── twse_cnn_model.hdf5             # TWSE 專用 CNN 深度學習辨識模型 (98%+)
├── setup_wizard.py                 # [本地精靈] 本地環境健康檢查與互動式設定精靈
├── verify_crawler.py               # 資料完整性驗證與自動補抓工具
├── test_cloud_merge.py             # 雲端 Parquet 與 Log 彙整單元測試腳本
├── parquet_to_excel.py             # Parquet 轉 Excel 工具
├── .github/workflows/
│   ├── daily_stock_crawler.yml     # 每日全市場定時爬蟲、分片日誌彙整與全市場二次聚合排程
│   ├── test_notification.yml       # Telegram 與 Email 晨間心跳與手動測試排程
│   └── test_gdrive.yml             # Google Drive 雲端同步一鍵手動驗證工作流程
├── stock_name_map.json             # 股票代碼與中文名稱快速快取對照檔
├── tpex_all_symbols.json           # 上櫃全市場標的代碼清單
├── 本地端執行指南.md               # 個人電腦 / 本地伺服器執行與 Windows 排程指南
├── GitHub_Secrets_申請與配置指南.md # GitHub Actions Secrets 完整申請指南
├── 台股分點爬蟲雲端排程設置指南.md # GitHub Actions 與伺服器定時排程指南
├── 外部排程網站觸發GitHub_Actions指南.md # 外部免費排程 (Cron-job.org) 秒級精準觸發指南
├── requirements.txt                # 專案 Python 依賴清單
├── 操作手冊.md                      # 本操作手冊
├── logs/                           # 執行日誌目錄
└── output/                         # 爬取完成之 Parquet / Excel / Log 資料庫目錄
    └── logs/                       # 彙整後之 YYYY-MM-DD-twse.log / YYYY-MM-DD-tpex.log
```

---

## 3. 環境建置與一鍵設定精靈

### 3.1 Python 版本建議
* 建議使用 **Python 3.10 ~ 3.12** (64-bit)。

### 3.2 一鍵啟動設定精靈 (最強烈推薦 ⭐)
```bash
cd stock_data_downloader
python setup_wizard.py
```
> 精靈會自動診斷本機 Python 套件、Google Chrome 與 CNN 模型，並以互動式選單引導填寫 Google Drive / Telegram / Email 設定，自動儲存至 `.env`！

---

## 4. 組態設定說明 (Config & Settings)

本系統支援豐富的命令列參數：
- `--date`：指定交易日期（格式 `YYYY-MM-DD`）。若未指定，系統會依據 **每日 17:00 換日規則** 自動推算最新交易日（17:00 前指向前一交易日，17:00 後自動指向當天）。
- `--market`：指定目標市場，可選 `all`（全市場）、`twse`（僅上市）、`tpex`（僅上櫃）。
- `--workers`：併發執行緒 / 進程數量（預設 `4` Workers 安全穩健，可設 `6` 或 `8` 極速衝刺）。
- `--max-rounds`：最大安全補抓輪數（預設 `6` 輪自適應安全降速補抓）。
- `--no-excel`：關閉自動匯出 Excel 檔（僅儲存 Parquet 格式以加快速度，極力推薦）。
- `--output-dir`：自訂輸出資料夾路徑。
- `--shard-id`：分散式矩陣分片索引（0-indexed）。
- `--num-shards`：分散式矩陣總分片數（例如 TWSE 設 6，TPEX 設 20）。
- **日誌記錄 (Logs)**：每次執行均會自動在 `logs/` 及 `output/logs/` 目錄建立檔案（單機為時間戳命名，分片模式自動命名為 `crawler_{market}_shard_{shard_id}.log`）。

---

## 5. 操作指令與使用範例

### 5.1 互動式設定與一鍵診斷精靈 (setup_wizard.py - 最強烈推薦)

```bash
python setup_wizard.py
```

---

### 5.2 一鍵下載全市場（上市 + 上櫃）分點日報表

```bash
# 預設抓取最近一個交易日 (極速 Parquet 模式，推薦)
python stock_crawler_coordinator.py --market all --workers 4 --no-excel

# 指定歷史日期抓取
python stock_crawler_coordinator.py --date 2026-08-26 --market all --workers 4 --no-excel
```

---

### 5.3 僅下載上市股票 (TWSE)

```bash
python stock_crawler_coordinator.py --market twse --workers 8 --no-excel
```

---

### 5.4 僅下載上櫃股票 (TPEX)

```bash
# 本地端將自動調用 tpex_crawler_local.py (4-Workers 多進程並行)
python stock_crawler_coordinator.py --market tpex --workers 4
```

---

### 5.5 雲端分散式矩陣分片執行與二次聚合架構 (Matrix Sharding & Merge)

在 GitHub Actions 雲端自動化排程中，系統採用三階段平行與聚合工作流：

```
[階段 1: 矩陣並行採集]
  ├─ TWSE 6-Runner (Shard 0~5)  ──> 生成 *_twse_shard_*.parquet & crawler_twse_shard_*.log
  └─ TPEX 20-Runner (Shard 0~19) ──> 生成 *_tpex_shard_*.parquet & crawler_tpex_shard_*.log

[階段 2: 分市場聚合與 Log 歸檔]
  ├─ merge-twse ──> 產出 api_absr1_*_twse.parquet (GDrive 根目錄)
  │             ──> 彙整 YYYY-MM-DD-twse.log (GDrive Log/ 資料夾)
  └─ merge-tpex ──> 產出 api_absr1_*_tpex.parquet (GDrive 根目錄)
                ──> 彙整 YYYY-MM-DD-tpex.log (GDrive Log/ 資料夾)

[階段 3: 全市場二次聚合與推播]
  └─ merge-all  ──> 合併上市與上櫃為 api_absr1_YYYY-MM-DD_YYYY-MM-DD.parquet (GDrive 根目錄)
                ──> 發送全市場整合報告至 Telegram & HTML Email
```

#### 手動執行分片聚合指令：
```bash
# 1. 聚合 TWSE 分片與彙整 TWSE 日誌
python merge_shards.py 2026-08-26 twse

# 2. 聚合 TPEX 分片與彙整 TPEX 日誌
python merge_shards.py 2026-08-26 tpex

# 3. 聚合全市場上市與上櫃 Parquet
python merge_shards.py 2026-08-26 all
```

---

### 5.6 驗證與自動補抓遺漏標的 (Verify & Repair)

```bash
python verify_crawler.py
```

---

### 5.7 Parquet 轉 Excel 工具 (parquet_to_excel.py)

```bash
python parquet_to_excel.py output/api_absr1_2026-08-26_2026-08-26.parquet
```

---

## 6. TWSE 雙引擎驗證碼辨識機制解析 (CNN vs ddddocr)

| 評比維度 | 專用 CNN 模型 (`twse_cnn_model.hdf5`) | 通用 `ddddocr` 備用引擎 |
| :--- | :--- | :--- |
| **定位與優先級** | ⭐ **第一優先級 (主力引擎)** | 🛡️ **第二優先級 (Fallback 備援)** |
| **TWSE 辨識準確率** | **98%+ (極高)** | **88% ~ 92% (良好)** |
| **處理方式** | OpenCV 預處理 + 5 字元切割辨識 | 端到端整圖推論 |
| **環境依賴** | TensorFlow / Keras, OpenCV | onnxruntime, ddddocr |
| **自適應容錯** | 辨識結果非 5 碼時自動切換至備援 | 接收 Fallback 請求保證不中斷 |

---

## 7. TPEX 上櫃雙環境拆分架構與對策 (Cloud vs Local)

```
                    ┌─────────────────────────┐
                    │  tpex_bsr_crawler.py    │
                    │  (統一 Gateway 路由閘道) │
                    └────────────┬────────────┘
                                 │
                 ┌───────────────┴───────────────┐
                 ▼                               ▼
    ┌─────────────────────────┐     ┌─────────────────────────┐
    │  tpex_crawler_cloud.py  │     │  tpex_crawler_local.py  │
    │  ☁️ GitHub Actions 專用 │     │  💻 Local 本地端專用     │
    ├─────────────────────────┤     ├─────────────────────────┤
    │ • 20-Shard 海外矩陣分片  │     │ • 4~8 Workers 多進程並行 │
    │ • 純淨單一持久會話       │     │ • 實體下載按鈕精準判定  │
    │ • 4 分鐘 Token 生命週期  │     │ • 斷點續傳快取 (秒級接關)│
    │ • 嚴格防封鎖熔斷防護    │     │ • 提速 400% ~ 600%      │
    └─────────────────────────┘     └─────────────────────────┘
```

---

## 8. 輸出結果與資料結構規格

### 8.1 Parquet 資料庫命名規範 (全市場 / 上市 / 上櫃)

抓取完成之檔案會儲存於 `output/` 目錄，並同步上傳至 Google Drive 根目錄：
- **全市場整合檔 (`all`)**：`api_absr1_YYYY-MM-DD_YYYY-MM-DD.parquet`
- **僅上市檔案 (`twse`)**：`api_absr1_YYYY-MM-DD_YYYY-MM-DD_twse.parquet`
- **僅上櫃檔案 (`tpex`)**：`api_absr1_YYYY-MM-DD_YYYY-MM-DD_tpex.parquet`

> 💡 **Parquet 壓縮規格**：全面採用 Meta **`ZSTD`** 高壓縮演算法。全市場 48 萬筆明細僅約 **11 MB**，極致節省空間與頻寬。

---

### 8.2 分片日誌 (Log) 彙整與 Google Drive Log 資料夾歸檔

系統會自動收集各節點的分片日誌，按 Shard 數值編號（Shard 0 至 5 / Shard 0 至 19）排序後合併為單一日誌檔案：
- **上市日誌**：`YYYY-MM-DD-twse.log`
- **上櫃日誌**：`YYYY-MM-DD-tpex.log`（同時生成相容檔 `YYYY-MM-DD-tpse.log`）
- **Google Drive 儲存位置**：自動存放於 Google Drive 根目錄下的 **`Log/`** 子資料夾（若資料夾不存在，系統會自動建立）。

---

### 8.3 標準 13 欄位定義

| 欄位名稱 | 型態 | 說明 | 範例 |
| :--- | :--- | :--- | :--- |
| `symbol` | `str` | 股票代碼 | `2330`, `6488`, `00679B` |
| `trade_date` | `str` | 交易日期 | `2026-08-26` |
| `broker_id` | `str` | 券商分點代碼 | `9200`, `9A00` |
| `buy_vol` | `float` | 買進股數 | `12500.0` |
| `sell_vol` | `float` | 賣出股數 | `12500.0` |
| `net_vol` | `float` | 買賣超股數 (`buy_vol - sell_vol`) | `0.0` |
| `buy_amt` | `float` | 買進金額 (千元) | `113.5` |
| `sell_amt` | `float` | 賣出金額 (千元) | `113.5` |
| `net_amt` | `float` | 淨買超金額 (`buy_amt - sell_amt`) | `0.0` |
| `buy_avg_price` | `float` | 買進加權均價 (元) | `9.08` |
| `sell_avg_price` | `float` | 賣出加權均價 (元) | `9.08` |
| `turnover` | `float` | 總成交金額 (千元) | `227.0` |
| `market_share` | `float` | 該分點佔該股成交佔比 (%) | `50.0` |

---

## 9. 雲端排程與 Secrets 密鑰申請指南

在 GitHub Actions 或本地 `.env` 中設定以下環境變數：

| 變數名稱 | 用途說明 | 範例與規格 |
| :--- | :--- | :--- |
| `GDRIVE_UPLOAD_URL` | **Google Apps Script Web App 網址** (推薦首選 ⭐) | `https://script.google.com/macros/s/.../exec` |
| `GDRIVE_FOLDER_ID` | Google Drive 目標資料夾 ID | 網址列 `folders/` 後方代碼 |
| `GDRIVE_LOG_FOLDER_ID` | Google Drive Log 目標資料夾 ID (選填，未填則自動建立 Log 子資料夾) | 網址列 `folders/` 後方代碼 |
| `GDRIVE_SERVICE_ACCOUNT_KEY` | GCP 服務帳戶 JSON 金鑰 (企業共用硬碟模式) | `{ "type": "service_account", ... }` |
| `SMTP_USER` | 寄件 Gmail 帳號 | `your_account@gmail.com` |
| `SMTP_PASSWORD` | Google 16 位應用程式密碼 | `xxxx xxxx xxxx xxxx` |
| `RECEIVER_EMAIL` | 接收 HTML 報表的 Email | `target_email@example.com` |
| `TELEGRAM_BOT_TOKEN` | Telegram 機器人 API Token | 由 Telegram `@BotFather` 提供 |
| `TELEGRAM_CHAT_ID` | Telegram 使用者或群組 ID | 由 Telegram `@userinfobot` 提供 |

> 💡 詳細申請教學請參閱：[`GitHub_Secrets_申請與配置指南.md`](file:///d:/MyProject/stock_data_downloader/GitHub_Secrets_申請與配置指南.md)

---

## 10. 常見問題排解 (FAQ & Troubleshooting)

### Q1: 全市場 Parquet 與上市/上櫃 Parquet 在 Google Drive 的存放位置為何？
> **解答**：
> 1. `api_absr1_YYYY-MM-DD_YYYY-MM-DD_twse.parquet`（上市獨立檔）➔ 存放在 Google Drive 根目錄。
> 2. `api_absr1_YYYY-MM-DD_YYYY-MM-DD_tpex.parquet`（上櫃獨立檔）➔ 存放在 Google Drive 根目錄。
> 3. `api_absr1_YYYY-MM-DD_YYYY-MM-DD.parquet`（全市場整合檔）➔ 存放在 Google Drive 根目錄。
> 4. `YYYY-MM-DD-twse.log` 與 `YYYY-MM-DD-tpex.log` ➔ 自動存放在 Google Drive 根目錄下的 **`Log/`** 子資料夾中。

### Q2: 為什麼分片 Log 需要按 Shard 數值排序？
> **解答**：若直接使用檔名字典排序，`shard_10` 會排在 `shard_2` 之前，造成日誌閱讀混亂。本系統使用自然數值排序演算法，保證 Shard 0 ~ 19 按順序嚴格排列，方便日誌追蹤與排錯。

### Q3: 系統的自動換日時間與 GitHub Actions 排程是幾點？
> **解答**：
> 1. **自動換日時間**：每日 **下午 17:00**（17:00 前執行自動抓取前一交易日，17:00 後執行自動抓取當天最新日報）。
> 2. **GitHub Actions 雲端雙排程機制**：
>    - 🚀 **主要排程**：每週一至週五 **台灣時間下午 17:37 (UTC 09:37)** 自動喚醒並行採集（避開整點高峰）。
>    - 🛡️ **二次備援排程**：每週二至週六 **台灣時間凌晨 05:31 (UTC 21:31)** 自動醒來檢查。
>    - ⚡ **3 秒極速快篩跳過**：備援排程啟動時會透過 `check_gdrive_exists.py` 先向 Google Drive 查詢該日資料庫；若昨晚已成功產檔，3 秒內標記為 `Skipped` 並結束，**零消耗 Runner 配額**；若發現缺檔則自動補抓！
