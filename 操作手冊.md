# 台股全市場數據採集與轉換系統 操作手冊 (User Manual)

本系統為個人與量化交易團隊設計之全市場台股數據採集、排程排程與格式轉換流水線。負責全市場每日「券商分點買賣日報彙總、每日收盤價、融資融券、期交所期貨未平倉與散戶小台籌碼，以及 TDCC 集保千張大戶持股分散」之自動化爬取、格式轉換與標準 Parquet 產出。

---

## 1. 系統簡介與核心功能

本系統整合五大台股核心資訊流與備援轉換工具：
1. **券商分點買賣日報彙總 (BSR / ABSR1)**：TWSE 證交所分點爬蟲 + TPEx 櫃買分點分散式爬蟲，產出 `api_absr1_*.parquet`。
2. **FinMind 逐價明細轉換工具 (`finmind_to_absr1.py`)**：將 FinMind 逐價買賣明細無損聚合為標準 `api_absr1_*.parquet`，提供官方爬蟲故障時的高效備援管線。
3. **每日收盤行情 (Close Price)**：TWSE + TPEx 全市場 14 欄位收盤價量，產出 `api_close1_*.parquet`。
4. **融資融券與券資比 (Margin Trading)**：全市場信用交易彙總，自動計算「資減、券增」及「券資比 (%)」，產出 `api_margin_*.parquet`。
5. **期交所台指期與散戶小台 (TAIFEX Futures)**：台指期貨三大法人未平倉量 + 散戶小台淨多空口數與多空比，產出 `api_taifex_*.parquet`。
6. **集保股權分散表 (TDCC Shareholding)**：全市場千張大戶持股比、50張以下散戶持股比及股東人數，產出 `api_tdcc_*.parquet`。

---

## 2. 專案目錄架構與模組清單

```
d:\MyProject\stock_data_downloader\
├── twse_crawler_v2.py              # 全新 TWSE 證交所分點爬蟲 v2 (支援本地斷點與雲端分片，已全面接入主工作流)
├── merge_twse_v2_shards.py         # TWSE 雲端分片 Parquet 合併模組 v2
├── twse_bsr_crawler.py              # TWSE 券商分點日報爬蟲 (已同步修復 ASP.NET 完整動態加密表單欄位)
├── tpex_crawler_cloud.py            # TPEx 櫃買分點分散式爬蟲
├── finmind_to_absr1.py              # FinMind 逐價分點轉換為標準 ABSR1 彙總表模組
├── close_price_crawler.py           # 每日收盤價爬蟲 (TWSE + TPEx)
├── margin_trading_crawler.py        # 每日融資融券爬蟲
├── taifex_futures_crawler.py        # 期交所期貨與散戶小台爬蟲
├── tdcc_shareholding_crawler.py     # TDCC 千張大戶股權分散爬蟲
├── trading_calendar.py              # 台股開休市判斷與行事曆
├── stock_crawler_coordinator.py     # 每日自動化協調排程入口
├── gdrive_sync.py                   # Google Drive 備份同步模組
├── check_progress.py                # 爬蟲進度即時監控工具
├── .github/workflows/
│   ├── daily_stock_crawler.yml      # 每日全市場主排程 (TWSE 已全面升級使用 twse_crawler_v2.py)
│   └── daily_stock_crawler_v2.yml   # 每日全市場券商分點爬蟲排程 v2 (TWSE 6-Runner v2 + TPEX 8-Runner 全市場整合)
├── output/                          # 預設 Parquet 輸出目錄
├── output_margin/                   # 融資融券 Parquet 存儲目錄
├── output_taifex/                   # 期交所期貨 Parquet 存儲目錄
├── output_tdcc/                     # 集保股權分散 Parquet 存儲目錄
├── docs/ai_planning/                # 架構規劃與設計規格書
├── requirements.txt                 # Python 依賴套件清單
└── 操作手冊.md                      # 本操作手冊
```

---

## 3. 環境建置與依賴安裝步驟

### 3.1 Python 環境需求
* **建議版本**：Python 3.10 或 3.11（Windows / Linux / macOS）
* **必要核心套件**：`pandas`, `pyarrow`, `requests`, `beautifulsoup4`, `lxml`

### 3.2 安裝步驟
```powershell
# 進入專案目錄
cd D:\MyProject\stock_data_downloader

# 安裝基本套件
pip install -r requirements.txt
pip install pyarrow pandas requests beautifulsoup4 lxml
```

---

## 4. 組態設定說明 (Config & Settings)

本系統轉換模組與各爬蟲皆設計為開箱即用，參數具備預設智慧推導：

| 參數名稱 | 預設值 | 說明 |
| :--- | :--- | :--- |
| `-i`, `--input` | `finmind_YYYY-MM-DD.parquet` | 來源 FinMind 檔案路徑（支援專案根目錄或 `output/` 目錄） |
| `-o`, `--output` | `output/api_absr1_YYYY-MM-DD_YYYY-MM-DD.parquet` | 輸出標準 ABSR1 Parquet 路徑 |
| `-d`, `--date` | `None` (自動讀取) | 指定特定交易日（YYYY-MM-DD），預設自動萃取資料內的交易日 |

---

## 5. 操作指令與使用範例

### 5.1 FinMind 逐價明細轉換為標準 ABSR1 彙總表 (`finmind_to_absr1.py`)

#### 1. 預設轉換（將根目錄之指定檔案轉至 output）
```powershell
py finmind_to_absr1.py -i D:\MyProject\stock_data_downloader\finmind_2026-09-17.parquet -o D:\MyProject\stock_data_downloader\output\api_absr1_2026-09-17_2026-09-17.parquet
```

#### 2. 簡便指令（在專案根目錄下直接執行）
```powershell
py finmind_to_absr1.py -i finmind_2026-09-17.parquet
```
*系統將自動解析內部交易日為 `2026-09-17`，並自動輸出到 `output/api_absr1_2026-09-17_2026-09-17.parquet`。*

#### 3. 指定日期過濾轉換
```powershell
py finmind_to_absr1.py -i all_finmind.parquet -d 2026-09-17
```

---

### 5.2 即時進度監控 (`check_progress.py`)
```powershell
py check_progress.py
```
顯示目前 TWSE/TPEx 採集進度、完成檔數、預估剩餘時間與最新採集之股票代碼。

---

### 5.3 全新一代 TWSE 上市分點爬蟲 v2 (`twse_crawler_v2.py`)

全新重構之 TWSE 獨立採集引擎，徹底擺脫舊版歷史包袱，專門為對抗證交所尖峰反爬與流量限流而設計。

#### 1. 操作指令範例
```powershell
# 1. 全市場斷點採集 (預設交易日為今日，每檔即時落盤，自動跳過已完成標的)
py twse_crawler_v2.py --date 2026-09-17

# 2. 定向補抓指定標的 (例如補跑權值股)
py twse_crawler_v2.py --date 2026-09-17 --symbols 2330,2317,2454,2382,2308,2881,2412,2882,2303,2891

# 3. 自訂安全間隔延遲 (例如 1.5 秒)
py twse_crawler_v2.py --date 2026-09-17 --delay 1.5
```

#### 2. 前後代 TWSE 爬蟲程式深度比較表 (v1 vs v2)
| 比較維度 | 第一代舊版 (`twse_bsr_crawler.py`) | 第二代全新版 (`twse_crawler_v2.py`) | 改進效益與核心差異 |
| :--- | :--- | :--- | :--- |
| **線程與併發架構** | 預設支援多線程 (8~12 Workers)，容易引發單 IP 併發碰撞 | **強制鎖定單線程 (1 Worker)**，節奏均勻一進一出 | 徹底根除並行請求引發的伺服器拒絕與連線碰撞 |
| **表單防偽簽章 (ViewState)** | 遺漏 `__VIEWSTATEENCRYPTED` 欄位 | **動態補齊 `__VIEWSTATEENCRYPTED`** 加密標記 | **徹底解決 236 bytes 純空白頁與 Silent Drop 拒絕現象** |
| **請求標頭 (Headers)** | 缺少 `Origin` 標頭，UA 為過期 Chrome 120 | **完整標頭 (`Origin`, `Referer`, Chrome 124)** | 100% 符合現代瀏覽器同源與 Fetch 安全規範 |
| **限流防禦機制** | 無知覺盲打，遇到空白頁仍以 0.3s 極限硬轟，導致 IP 被死鎖 | **智慧熔斷冷卻器 (Circuit Breaker)**：連續命中限流即自動休眠 75 秒，探針驗證解封才接續 | 智能踩剎車保護 IP 安全，杜絕將伺服器打到黑名單 |
| **節奏與頻率控制** | 死板固定 0.3s 極限延遲，極易踩到站方流量閥值 | **1.0s ~ 1.4s 浮動安全延遲 + 每 50 檔小休呼吸 3 秒** | 完美模擬真人正常瀏覽特徵，大幅降低被 WAF 識別風險 |
| **進度持久化 (儲存模式)** | 全部累積於記憶體 List，全輪跑完才存檔，**中斷則前功盡棄** | **原生斷點續傳 (Checkpointing)**：每檔成功即時落盤 Parquet 快取 | **隨時按 Ctrl+C 中斷，下次啟動 0 秒跳過已完成標的** |
| **控制台反饋 (Console UI)** | 終端僅印出簡略訊息或日誌被緩衝延遲 | **專業即時儀表板** (百分比、即時速度、剩餘時間、累積筆數、零延遲 `flush`) | 執行狀態一目了然，隨時掌握全市場推進速度 |
| **實體日誌 (File Log)** | 依賴 Coordinator 外部包裝 | **內建 FileHandler**，自動落地至 `logs/crawler_twse_v2_*.log` | 每次執行軌跡完整歸檔，方便日後查閱與排錯 |
| **全市場實測表現** | 跑到 1020 檔死鎖，技術待補飆升 456 檔，成功數凍結 | **全市場 1,262 檔持續推進，成功率 100.0% 零失敗** | 從頻繁撞牆蛻變為高可靠性生產級爬蟲 |

#### 3. 雲端 GitHub Actions 排程 (v2)
* **專屬獨立 Workflow**：`.github/workflows/daily_stock_crawler_v2.yml`（完全不改動舊版 `daily_stock_crawler.yml`，0 風險雙軌並行）。
* **矩陣式並行 Runner**：
  * 上市全市場由 6 個 Runner 分工。
  * 上櫃由 8 個 Runner 進行錯峰採集。
* **分片合併與全市場整合 (`merge-all`)**：
  * 由 `merge_shards.py` 將上市與上櫃總表整合成單一全市場母表 `api_absr1_YYYY-MM-DD_YYYY-MM-DD.parquet`。
  * **雙通道即時推播**：整合完成後自動發送詳細 HTML 視覺化 Email 報表與 Telegram 統計訊息。
  * **自動雲端備份**：母表、收盤價與日誌自動備份至 Google Drive（具備防重複上傳機制），並於 GitHub Artifacts 保留 7 天供下載。

---

## 6. 輸出結果與報表解讀

轉換產出的 `api_absr1_*.parquet` 採用標準 PyArrow SNAPPY 壓縮，擁有與官方證交所/櫃買完全一致的 13 欄位：

| 欄位名稱 | 型態 | 單位 | 說明與報表解讀意義 |
| :--- | :--- | :--- | :--- |
| `symbol` | string | - | 股票代碼 (例: 2330) |
| `trade_date` | string | - | 交易日期 (YYYY-MM-DD) |
| `broker_id` | string | - | 券商分點代碼 (4碼，例如 1020 為合庫) |
| `buy_vol` | double | 股 | 該分點當日對該股的總買進股數 |
| `sell_vol` | double | 股 | 該分點當日對該股的總賣出股數 |
| `net_vol` | double | 股 | 買賣超股數 (`buy_vol - sell_vol`)。正值為買超，負值為賣超 |
| `buy_amt` | double | 千元 | 該分點買進總金額 |
| `sell_amt` | double | 千元 | 該分點賣出總金額 |
| `net_amt` | double | 千元 | 該分點淨買賣金額 (`buy_amt - sell_amt`) |
| `buy_avg_price` | double | 元 | 該分點當日買進加權均價 |
| `sell_avg_price` | double | 元 | 該分點當日賣出加權均價 |
| `turnover` | double | 千元 | 該分點當日總成交金額 (`buy_amt + sell_amt`) |
| `market_share` | double | % | 該分點佔該檔股票全市場成交量之市佔率百分比 |

---

## 7. 常見問題排解 (FAQ & Troubleshooting)

### Q1: 為什麼由 FinMind 轉換出來的筆數會與原始 `finmind_*.parquet` 不同？
* **解答**：這是正常的。原始 `finmind` 檔案是**「逐成交價位明細」**（同一分點在 2410、2420、2430 等不同價位會有獨立列），筆數多達 240 萬筆；經 `finmind_to_absr1.py` 彙總後，會依據「股票 + 分點」進行加總整合為 1 列（約 52 萬筆），計算出精確的加權均價與買賣超，完全符合籌碼分析所需的標準規格。

### Q2: 轉換時出現 `MemoryError` 或記憶體不足？
* **解答**：`finmind_to_absr1.py` 採用全向量化 Pandas/PyArrow 聚合，240 萬列約僅消耗 500MB ~ 800MB 記憶體。若在受限環境（如極低記憶體 VM），建議指定 `-d YYYY-MM-DD` 單日過濾處理。

### Q3: 輸出的 Parquet 能否直接給 `stock_data_analysis` 籌碼吸籌模型讀取？
* **解答**：可以！經 PyArrow Schema 嚴格比對，輸出欄位、型態（`string` 與 `double`）、缺失值規則均與證交所原裝 `api_absr1` 完全一致，分析系統可無縫銜接讀取。

### Q4: 雲端 TPEX 上櫃爬蟲如何確保穩定採集全市場 1,006 檔標的？
* **解答**：
  1. **全市場標的完整涵蓋**：系統已全面支援全市場 **1,006 檔上櫃與興櫃標的**（包含 851 檔主板個股、118 檔上櫃 ETF 與債券 ETF、以及 37 檔興櫃個股），所有具備券商買賣分點成交記錄之標的均 100% 完整納入採集範圍，絕不遺漏。
  2. **串行防風控機制**：TPEX 前端伺服器對並發極度敏感，雲端排程嚴格採取 **`max-parallel: 1`** 單路串行執行，並搭配 15 秒錯峰啟動與多輪溫和收斂，確保不會因爭搶 Session 觸發全站逾時。

### Q5: 雲端 TWSE 上市分片爬蟲若卡在某一檔（如進度 34/229）長久無輸出，原因為何？如何解決？
* **原因剖析**：
  在向證交所（`bsr.twse.com.tw`）發送 HTTP 請求時，若證交所伺服器發生 TCP 半開或未正常關閉的連線懸掛（Stalled Socket），底層 `requests` 容易陷入阻塞，導致 GitHub Actions 雲端 Runner 靜默無輸出直到逾時。
* **防護與解決機制**：
  1. **單檔外層硬性超時守護 (Hard Timeout Guard)**：系統在 `fetch_single_symbol()` 內建 `concurrent.futures` 執行緒守護，嚴格限制單檔不得超過 25 秒，逾時立即強制中斷並列入補抓名單，主迴圈絕不卡死。
  2. **雙元組超時與顯式關閉**：所有網路請求強制設定 `timeout=(4.0, 8.0)`（連線 4 秒、讀取 8 秒），並於 `finally` 區塊顯式呼叫 `session.close()` 釋放連線池。
  3. **一鍵補跑**：若 GitHub Actions 偶發單一分片失敗，無須整批重跑，直接在該次 Run 頁面點擊 **「Re-run failed jobs」**，即可單獨重跑失敗的分片並自動觸發下游合併。

### Q6: 雲端 TPEX 上櫃分片啟動時若出現「首頁 Turnstile 初始 Token 未取得」，如何避免分片直接崩潰？
* **原因剖析**：
  TPEX 櫃買中心日報表頁面的 Cloudflare Turnstile 驗證在海外 GitHub Actions 環境下，有時處於靜止狀態，必須等使用者於前端輸入股票代號或點擊按鈕時才會動態觸發簽發。若系統在瀏覽器剛開立首頁時強行檢驗 Token 長度，容易誤判為 Session 預熱失敗。
* **防護與解決機制**：
  1. **DOM 結構就緒彈性放行**：系統在 `_launch_browser_session()` 引入彈性放行機制，只要首頁 DOM 結構（如 `input.code` 輸入框或表單）載入成功，即認定 Session 可用；若首頁未即時拿到 Token，會自動延後至單檔查詢時由 `_wait_token()` 動態簽發，不再拋出 `RuntimeError` 造成分片中斷。
  2. **全面安全兜底保護**：`crawl_stocks()` 移除頂層盲目 re-raise，所有未完成標的皆會安全落盤並加入補抓清單；`stock_crawler_coordinator.py` 同步增加頂層例外捕捉防護，杜絕分片程序無端 Crash。

### Q7: 全市場母表整合 (merge-all) 出現「未找到任何待聚合的 ALL 分片檔案」，如何解決？
* **原因剖析**：
  GitHub Actions `actions/download-artifact@v4` 若使用萬用字元（`pattern: "*-final-parquet"`），預設會將不同產物分別解壓縮至獨立子目錄（如 `output/twse-final-parquet/` 與 `output/tpex-final-parquet/`）。若聚合腳本僅在 `output/` 根目錄搜尋，將因檔案被包在子層而判定為未找到檔案。
* **防護與解決機制**：
  1. **遞迴搜尋與自動扁平化**：`merge_shards.py` 已升級全市場模式搜尋邏輯，自動遞迴遍歷 `output/` 底下所有子層目錄並自動複製至頂層，徹底消除目錄層級差異。
  2. **Workflow 多產物合併設定**：在 `download-artifact@v4` 明確加入 `merge-multiple: true`，直接將所有最終產物展平解壓至同一層目錄，雙重保險確保 100% 成功對齊。



