# 2026-09-16 TWSE 採集異常診斷與應變紀錄

## 日期
- 2026-09-16

## 背景
2026-09-16 當日，TWSE 上市券商分點採集在本地端與雲端分片環境皆出現成功率明顯下降、補抓輪次增加、部分批次無法在預期時間內完成的現象。相較於 2026-09-15 可正常完成全量 TWSE 採集，當日異常屬於突發退化。

## 當日觀察到的主要現象
1. 本地端與雲端分片均出現相同失敗型態，不屬於單機環境故障。
2. 失敗原因主要集中於：
   - `menu_http_error`
   - `post_missing_download_link`
3. 小批次、低併發測試有機會成功，但全量與高併發時失敗比例明顯上升。
4. `TensorFlow GPU support is not available` 僅為 Windows 原生 TensorFlow 提示，影響的是運算加速能力，不是本次失敗主因。

## 初步判斷
本次問題較可能來自 TWSE BSR 查詢鏈路對流量型態變得更敏感，而非單純 OCR 模型故障。綜合日誌、即時探測與小批次測試結果，判斷如下：

1. **不是純 OCR 問題**
   - CNN 模型可正常載入。
   - 若為純 OCR 故障，小批次低併發測試不應仍能偶爾成功。

2. **較像站方風控/節流加嚴**
   - 雲端與本地同時出現 `menu_http_error`。
   - 完整查詢流程常停在 POST 後未生成下載連結，對應 `post_missing_download_link`。
   - 高併發時異常明顯放大，低併發時明顯改善。

3. **平行度是放大因子**
   - 雲端原本 TWSE 採用 6-shard * 4 workers，理論上會形成較高併發壓力。
   - 本地改為更保守模式後，前段成功率明顯回升。

## 實際 log 範例摘要

### 範例 A：2026-09-15 正常完成全量 TWSE
來源：
- `output/logs/crawler_all_20260915_163620.log`

關鍵摘要：
```text
[2026-09-15 17:25:14] [✓] TWSE 上市採集完成：共分配 1367 檔 | 有效成交產出: 1367 檔 | 無成交/略過: 0 檔 | 失敗: 0 檔
```

判讀：
- 2026-09-15 前一交易日 TWSE 全量採集可正常完成。
- 可作為 2026-09-16 異常退化的對照基準。

### 範例 B：2026-09-16 晚間全量 + 2 Workers，前 30 檔即觸發嚴格偵測
來源：
- `output/logs/crawler_all_20260916_224126.log`

關鍵摘要：
```text
[22:42:14] [第1輪 進度 15/1371 (1.1%)] 成功: 0 檔 | 無交易/待補: 15 檔 | 累積: 0 筆 | 速度: 0.3 檔/s | 剩餘約: 71.9 分鐘
[22:43:02] [第1輪 進度 30/1371 (2.2%)] 成功: 0 檔 | 無交易/待補: 30 檔 | 累積: 0 筆 | 速度: 0.3 檔/s | 剩餘約: 71.4 分鐘
[22:43:02] [TWSE 嚴格偵測] TWSE 嚴格偵測命中：前 30 檔成功率僅 0.0%，無成交比率 0.0%，技術性異常比率 100.0%；主因 menu_http_error=18, post_missing_download_link=12
```

判讀：
- 高併發或較高請求密度條件下，TWSE 當晚曾出現前段全面失敗。
- 失敗原因並非單一 captcha 錯誤，而是 `menu_http_error` 與 `post_missing_download_link` 同時升高。

### 範例 C：2026-09-16 同日晚間小批次 + 1 Worker，可恢復成功
來源：
- `output/logs/crawler_twse_20260916_223821.log`

關鍵摘要：
```text
[22:39:31] [第1輪 進度 15/20 (75.0%)] 成功: 9 檔 | 無交易/待補: 6 檔 | 累積: 4,513 筆 | 速度: 0.2 檔/s | 剩餘約: 0.4 分鐘
[22:39:39] [第1輪 進度 20/20 (100.0%)] 成功: 14 檔 | 無交易/待補: 6 檔 | 累積: 6,156 筆 | 速度: 0.3 檔/s | 剩餘約: 0.0 分鐘
[22:39:57] [+] 全市場標的 100% 抓取達成！(共執行 2 輪)
[2026-09-16 22:39:57] [✓] TWSE 上市採集完成：共分配 20 檔 | 有效成交產出: 20 檔 | 無成交/略過: 0 檔 | 失敗: 0 檔
[2026-09-16 22:39:57] [*] TWSE 技術性失敗診斷 Top 2: post_missing_download_link=5, menu_http_error=1
```

判讀：
- 同一天在較低併發條件下，TWSE 採集可回復到可用狀態。
- 支持「平行度/請求密度是放大因子」這一判斷。

### 範例 D：2026-09-16 本地低併發重跑，前段成功率明顯改善
來源：
- 本地端即時輸出（23:39 ~ 23:41）

關鍵摘要：
```text
[23:39:52] [第1輪 進度 15/1371 (1.1%)] 成功: 15 檔 | 明確無資料: 0 檔 | 技術待補: 0 檔 | 累積: 7,608 筆 | 速度: 0.4 檔/s | 剩餘約: 64.3 分鐘
[23:40:31] [第1輪 進度 30/1371 (2.2%)] 成功: 29 檔 | 明確無資料: 0 檔 | 技術待補: 1 檔 | 累積: 11,059 筆 | 速度: 0.4 檔/s | 剩餘約: 61.1 分鐘
[23:41:30] [第1輪 進度 45/1371 (3.3%)] 成功: 34 檔 | 明確無資料: 0 檔 | 技術待補: 11 檔 | 累積: 11,631 筆 | 速度: 0.3 檔/s | 剩餘約: 69.0 分鐘
[23:41:58] [第1輪 進度 60/1371 (4.4%)] 成功: 49 檔 | 明確無資料: 0 檔 | 技術待補: 11 檔 | 累積: 13,543 筆 | 速度: 0.4 檔/s | 剩餘約: 61.3 分鐘
```

判讀：
- 在新的狀態分流日誌下，可直接看出前段不是全面失敗。
- 低併發策略至少能明顯降低一開局就全面被拒絕的機率。

## 當日完成的程式修改

### 1. TWSE 採集狀態與失敗原因分流
修改檔案：
- `twse_bsr_crawler.py`
- `stock_crawler_coordinator.py`

調整內容：
1. 將 TWSE 單檔抓取結果改為明確區分：
   - `success`
   - `no_data`
   - `technical_failure`
2. 針對技術性失敗記錄更細原因，例如：
   - `menu_http_error:*`
   - `menu_missing_form_fields`
   - `captcha_http_error:*`
   - `captcha_recognition_failed`
   - `captcha_validation_failed`
   - `post_missing_download_link`
   - `content_http_error:*`
   - `content_invalid_payload`
3. 進度列由原先的「無交易/待補」改為：
   - 成功
   - 明確無資料
   - 技術待補
4. 對於已確認 `no_data` 的標的不再重複進入補抓輪次。
5. 在協調器摘要中額外輸出 `TWSE 技術失敗主因 Top N`，方便快速辨識是站方阻擋、驗證流程還是內容頁問題。

### 2. 本地端 Ctrl+C 可中斷能力補強
修改檔案：
- `setup_wizard.py`
- `twse_bsr_crawler.py`
- `stock_crawler_coordinator.py`

調整內容：
1. `setup_wizard.py` 新增可中斷子程序執行器：
   - 改用 `Popen(...)` 啟動子程序。
   - 父程序接收到 `Ctrl+C` 後，主動通知子程序中止。
   - 若子程序未於短時間內結束，再進一步強制終止。
2. `twse_bsr_crawler.py` 新增停止旗標：
   - `request_stop()`
   - `_sleep_or_stop()`
3. TWSE 抓取與補抓 thread pool 在收到 `KeyboardInterrupt` 後：
   - 停止排入後續任務
   - 取消尚未開始的 futures
   - 中止後續補抓輪次
4. `stock_crawler_coordinator.py` 補上協調器層級的中止訊息與結束碼，讓本地端中斷流程更清楚。

### 3. 雲端 TWSE 分片降載
修改檔案：
- `.github/workflows/daily_stock_crawler.yml`

調整內容：
1. 保留 6 個 TWSE shard。
2. 將每個 shard 的內部 worker 數由 `4` 下修為 `1`。
3. 目的為降低對 TWSE BSR 站點的瞬時併發壓力，驗證高併發是否為當日異常的重要放大因子。

## 當日操作結論
1. TWSE 2026-09-16 的退化現象同時出現在 local 與 cloud，較可能是站方當日行為改變或風控加嚴。
2. 降低併發後，前段成功率有恢復跡象，支持「平行度會放大失敗率」的判斷。
3. 目前最合理的保守策略為：
   - 本地端以低併發或單 worker 進行 TWSE 採集。
   - 雲端保留分片，但每 shard 固定 1 worker。
   - 先觀察 2026-09-17 後的日誌，確認是否為單日事件或新常態。

## 後續觀察重點
1. 2026-09-17 起是否仍大量出現 `menu_http_error`。
2. 低併發情況下 `post_missing_download_link` 是否顯著下降。
3. 補抓輪次最終能否將多數 `technical_failure` 回收。
4. 是否需要進一步把 TWSE 雲端模式收斂為：
   - 6 shards / 1 worker
   - 2 shards / 1 worker
   - 或單機單線完整慢跑

## 2026-09-17 補充驗證：1541（錩泰）屬於 `no_data`，不是剩餘技術失敗
1. 針對 2026-09-16 本地全市場執行殘留的 1541（錩泰），以 `retry_failed_twse_symbols.py` 搭配 `--workers 1 --max-rounds 10` 進行單檔重跑後，仍反覆出現 `post_missing_download_link` 與 `captcha_validation_failed`。
2. 使用者手動開啟 TWSE BSR 查詢頁並查詢同日同標的後，網站前端實際顯示 `查無資料`，且明細區為空表格。
3. 這表示 1541 在該日更合理的分類是「網站正常回應，但本日無可下載資料」，而不是 `technical_failure`。
4. 因此後續程式修正方向不是持續增加 1541 的補抓次數，而是把 TWSE 回應中的 `查無資料` 文案納入 `no_data` 判定，避免補抓工具反覆重跑正常空結果頁。
5. 另因自動化重跑與人工畫面仍可能不一致，後續再補上「單檔診斷模式」：保存每次 POST 回應 HTML 與摘要 JSON，避免只有最終 reason 而看不到中間真實頁面。

## 相關檔案
- `twse_bsr_crawler.py`
- `stock_crawler_coordinator.py`
- `setup_wizard.py`
- `.github/workflows/daily_stock_crawler.yml`
- `程式歷史紀錄檔.md`
