# 💻 台股券商分點爬蟲系統 — 本地端執行與開發指南 (Local Run Guide)

本文件詳細說明如何在**個人電腦 / 本地伺服器環境（Windows / macOS / Linux）**中完整運行台股分點爬蟲、執行驗證碼辨識、自動產出 Parquet / Excel 資料庫，以及設定 Windows 工作排程器或 Linux Crontab 自動化。

---

## 📌 目錄
1. [系統環境與前置準備](#1-系統環境與前置準備)
2. [本地依賴套件安裝](#2-本地依賴套件安裝)
3. [環境變數與組態設定 (.env)](#3-環境變數與組態設定-env)
4. [核心執行指令與情境範例](#4-核心執行指令與情境範例)
   - [4.1 一鍵採集全市場（上市 + 上櫃）](#41-一鍵採集全市場上市--上櫃)
   - [4.2 僅採集上市股票 (TWSE)](#42-僅採集上市股票-twse)
   - [4.3 僅採集上櫃股票 (TPEX)](#43-僅採集上櫃股票-tpex)
   - [4.4 歷史特定日期採集與補抓](#44-歷史特定日期採集與補抓)
5. [輔助工具與連線測試](#5-輔助工具與連線測試)
   - [5.1 本地驗證 Google Drive 雲端同步](#51-本地驗證-google-drive-雲端同步)
   - [5.2 本地測試 Telegram / Email 報表](#52-本地測試-telegram--email-報表)
   - [5.3 資料完整性核對與自動修復 (verify_crawler.py)](#53-資料完整性核對與自動修復-verify_crawlerpy)
   - [5.4 Parquet 高速批次轉 Excel (parquet_to_excel.py)](#54-parquet-高速批次轉-excel-parquet_to_excelpy)
6. [本地自動排程設置教學](#6-本地自動排程設置教學)
   - [6.1 Windows 工作排程器 (Task Scheduler)](#61-windows-工作排程器-task-scheduler)
   - [6.2 Linux / macOS 定時排程 (Crontab)](#62-linux--macos-定時排程-crontab)
7. [常見問題排解 (FAQ)](#7-常見問題排解-faq)

---

## 1. 系統環境與前置準備

### 1.1 支援作業系統
* **Windows 10 / 11**（推薦使用 PowerShell 或 VS Code 終端機）
* **Linux (Ubuntu 20.04 / 22.04 / 24.04 等)**
* **macOS (Apple Silicon M1/M2/M3 或 Intel)**

### 1.2 Python 版本要求
* **建議版本**：`Python 3.10` ~ `3.12` (64-bit)。
* 請先至 [Python 官方網站](https://www.python.org/downloads/) 下載安裝，並勾選「**Add python.exe to PATH**」。

### 1.3 瀏覽器環境 (供 TPEX 上櫃爬蟲使用)
* 本地端需安裝 **Google Chrome 瀏覽器**。TPEX 模組會自動調用本機 Chrome 核心執行無頭或視覺化操作。

---

## 2. 本地依賴套件安裝與一鍵設定精靈

### 🚀 方式一：一鍵啟動互動式設定精靈 (最強烈推薦 ⭐)
專案內建專屬的 **`setup_wizard.py`** 互動精靈，自動檢查環境並引導完成所有設定：

```bash
cd d:\MyProject\stock_data_downloader
python setup_wizard.py
```
> 執行後會自動診斷：
> 1. Python 版本與所有依賴套件是否就緒
> 2. Google Chrome 瀏覽器是否就緒
> 3. TWSE CNN 深度學習模型是否齊全
> 4. 互動式輸入 Google Drive / Telegram / Email 參數並自動生成 `.env`
> 5. 內建一鍵執行連線測試與爬蟲任務選單！

---

### 方式二：手動安裝依賴與設定

```bash
# 升級 pip 並安裝專案依賴
python -m pip install --upgrade pip
pip install -r requirements.txt
```

---

## 3. 環境變數與組態設定 (.env)

若您希望在本地執行時也能自動將成果備份至 Google Drive 或發送 Telegram / Email 通知，可在專案根目錄建立 `.env` 檔案或於系統環境變數中設定（選填，若未設定則僅在本地儲存檔案）：

在專案目錄下新增 `.env` 檔案：
```ini
# Google Drive 雲端同步 (二選一)
GDRIVE_UPLOAD_URL=https://script.google.com/macros/s/AKfycb.../exec
GDRIVE_FOLDER_ID=1L6f9j...h_0a

# Telegram 即時推播 (選填)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGhIJKlmNoPQRsTUVwxyZ
TELEGRAM_CHAT_ID=1234567890

# SMTP 郵件報表 (選填)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_16_digit_app_password
RECEIVER_EMAIL=your_email@gmail.com
```

---

## 4. 核心執行指令與情境範例

所有採集指令皆透過核心入口程式 [`stock_crawler_coordinator.py`](file:///d:/MyProject/stock_data_downloader/stock_crawler_coordinator.py) 調度執行：

### 4.1 一鍵採集全市場（上市 + 上櫃）
自動判斷最新交易日，執行上市高併發採集 + 5 輪安全降速補抓 + 上櫃自動化採集，產出完整資料庫：

```bash
# 自動判斷最新交易日，產出 Parquet 與 Excel
python stock_crawler_coordinator.py

# 極速模式：僅產出 Parquet 資料庫（不輸出 Excel，速度提升 50%）
python stock_crawler_coordinator.py --no-excel
```

---

### 4.2 僅採集上市股票 (TWSE)
使用 CNN 深度學習破除驗證碼，支援自訂線程數與最大補抓輪數：

```bash
# 預設 8 線程極速採集
python stock_crawler_coordinator.py --market twse

# 自訂 4 線程、最大補抓 6 輪
python stock_crawler_coordinator.py --market twse --workers 4 --max-rounds 6
```

---

### 4.3 僅採集上櫃股票 (TPEX)
自動啟動 Chrome 瀏覽器會話抗限流採集全市場 1,011 檔上櫃個股：

```bash
python stock_crawler_coordinator.py --market tpex
```

---

### 4.4 歷史特定日期採集與補抓
支援指定過去任意交易日期（格式為 `YYYY-MM-DD`）：

```bash
# 採集指定歷史日期全市場資料
python stock_crawler_coordinator.py --date 2026-08-21 --market all

# 僅採集指定歷史日期的上市股票
python stock_crawler_coordinator.py --date 2026-08-21 --market twse
```

---

## 5. 輔助工具與連線測試

專案內建多項獨立實用工具，方便本地端除錯與驗證：

### 5.1 本地驗證 Google Drive 雲端同步
```bash
python test_gdrive.py
```
> 執行 4 階段連線檢查，確認 Google Apps Script 或 Service Account 是否能順利將檔案存入目標 Google Drive 資料夾。

---

### 5.2 本地測試 Telegram / Email 報表
```bash
python test_notify.py
```
> 發送一則測試報表至您的 Telegram 與 Email，確認通訊管道暢通。

---

### 5.3 資料完整性核對與自動修復 (`verify_crawler.py`)
採集完成後，若想確認是否有任何標的遺漏並自動對缺漏股票進行原地修復：

```bash
# 驗證最新交易日資料庫完整性
python verify_crawler.py

# 驗證指定歷史日期
python verify_crawler.py --date 2026-08-21
```

---

### 5.4 Parquet 高速批次轉 Excel (`parquet_to_excel.py`)
若平時習慣使用 Excel 開啟分析，可使用內建的高效轉換工具：

```bash
# 1. 將 output 目錄下所有 Parquet 檔案批次轉為 Excel (.xlsx)
python parquet_to_excel.py output

# 2. 將單一指定 Parquet 轉成 Excel
python parquet_to_excel.py output/api_absr1_2026-08-21_2026-08-21.parquet
```

---

## 6. 本地自動排程設置教學

若您想讓個人電腦或本地伺服器每天在台股盤後（約晚上 20:30）全自動執行：

### 6.1 Windows 工作排程器 (Task Scheduler)

1. 按 `Win + R` 輸入 `taskschd.msc` 開啟「工作排程器」。
2. 點選右側 **「建立工作」**：
   * **一般**：名稱輸入 `台股分點自動採集`，勾選「以最高權限執行」。
   * **觸發程序**：新增 ➔ 排程選擇「每週」，勾選週一至週五，時間設定為 `20:30:00`。
   * **動作**：新增 ➔ 動作選擇「啟動程式」：
     * **程式或指令碼**：填入 Python 執行檔路徑，例如：
       `D:\MyProject\stock_data_downloader\venv\Scripts\python.exe`
     * **新增引數 (選填)**：
       `stock_crawler_coordinator.py --market all --no-excel`
     * **開始位置 (重要 ⚠️)**：填入專案根目錄路徑：
       `D:\MyProject\stock_data_downloader`
3. 點擊確定儲存。

---

### 6.2 Linux / macOS 定時排程 (Crontab)

在終端機輸入 `crontab -e` 並新增排程：

```bash
# 每週一至週五 台灣時間 20:30 自動採集全市場分點
30 20 * * 1-5 cd /path/to/stock_data_downloader && /path/to/stock_data_downloader/venv/bin/python stock_crawler_coordinator.py --market all --no-excel >> /path/to/stock_data_downloader/crawler.log 2>&1
```

---

## 7. 常見問題排解 (FAQ)

### Q1: 執行時報錯 `ModuleNotFoundError: No module named 'tensorflow'`？
* **解答**：請確認是否已啟動虛擬環境並安裝完整依賴：
  ```bash
  pip install -r requirements.txt
  ```

### Q2: TPEX 上櫃爬蟲啟動時提示 Chrome 崩潰或找不到路徑？
* **解答**：請確認電腦已安裝最新版 Google Chrome 瀏覽器。DrissionPage 模組會自動偵測預設安裝路徑（Windows 預設路徑 `C:\Program Files\Google\Chrome\Application\chrome.exe`）。

### Q3: 採集出來的 Parquet 檔案儲存在哪裡？
* **解答**：所有成果檔案均統一保存在專案目錄下的 **`output/`** 資料夾中，檔名格式為 `api_absr1_YYYY-MM-DD_YYYY-MM-DD.parquet`。

### Q4: 如何在沒有 GPU 的一般筆電上運行？
* **解答**：專案內建的 TWSE CNN 驗證碼辨識模型經過輕量化優化，純 CPU 推論單張僅需 **5 ~ 10 毫秒**，一般文書筆電即可順暢高速運行。
