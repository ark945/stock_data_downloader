# 💻 台股券商分點爬蟲系統 — 本地端執行與開發指南 (Local Run Guide)

本文件詳細說明如何在**個人電腦 / 本地伺服器環境（Windows / macOS / Linux）**中完整運行台股分點爬蟲、執行驗證碼辨識、自動產出 Parquet / Excel 資料庫，以及設定 Windows 工作排程器或 Linux Crontab 自動化。

---

## 📌 目錄
1. [系統環境與前置準備](#1-系統環境與前置準備)
2. [本地依賴套件安裝與一鍵設定精靈](#2-本地依賴套件安裝與一鍵設定精靈)
3. [環境變數與組態設定 (.env)](#3-環境變數與組態設定-env)
4. [核心執行指令與情境範例](#4-核心執行指令與情境範例)
   - [4.1 一鍵採集全市場（上市 + 上櫃）](#41-一鍵採集全市場上市--上櫃)
   - [4.2 僅採集上市股票 (TWSE)](#42-僅採集上市股票-twse)
   - [4.3 僅採集上櫃股票 (TPEX - 本地多進程加速)](#43-僅採集上櫃股票-tpex---本地多進程加速)
   - [4.4 歷史特定日期採集與補抓](#44-歷史特定日期採集與補抓)
5. [本地端 TPEX 專屬對策與斷點續傳機制](#5-本地端-tpex-專屬對策與斷點續傳機制)
6. [輔助工具與連線測試](#6-輔助工具與連線測試)
   - [6.1 本地驗證 Google Drive 雲端同步](#61-本地驗證-google-drive-雲端同步)
   - [6.2 本地測試 Telegram / Email 報表](#62-本地測試-telegram--email-報表)
   - [6.3 資料完整性核對與自動修復 (verify_crawler.py)](#63-資料完整性核對與自動修復-verify_crawlerpy)
   - [6.4 Parquet 高速批次轉 Excel (parquet_to_excel.py)](#64-parquet-高速批次轉-excel-parquet_to_excelpy)
7. [本地自動排程設置教學](#7-本地自動排程設置教學)
   - [7.1 Windows 工作排程器 (Task Scheduler)](#71-windows-工作排程器-task-scheduler)
   - [7.2 Linux / macOS 定時排程 (Crontab)](#72-linux--macos-定時排程-crontab)
8. [常見問題排解 (FAQ)](#8-常見問題排解-faq)

---

## 1. 系統環境與前置準備

### 1.1 支援作業系統
* **Windows 10 / 11**（推薦使用 PowerShell 或 VS Code 終端機）
* **Linux (Ubuntu 20.04 / 22.04 / 24.04 等)**
* **macOS (Apple Silicon M1/M2/M3 或 Intel)**

### 1.2 Python 版本要求
* **建議版本**：`Python 3.10` ~ `3.12` (64-bit)。
* 請先至 [Python 官方網站](https://www.python.org/downloads/) 下載安裝，並勾選「**Add python.exe to PATH**」。

### 1.3 瀏覽器環境 (供 TPEX 上櫃爬蟲使用)
* 本地端需安裝 **Google Chrome 瀏覽器**。TPEX 模組會自動調用本機 Chrome 核心執行無頭或視覺化操作。

---

## 2. 本地依賴套件安裝與一鍵設定精靈

### 🚀 方式一：一鍵啟動互動式設定精靈 (最強烈推薦 ⭐)
專案內建專屬的 **`setup_wizard.py`** 互動精靈，自動檢查環境並引導完成所有設定：

```bash
cd d:\MyProject\stock_data_downloader
python setup_wizard.py
```
> 執行後會自動診斷：
> 1. Python 版本與所有依賴套件是否就緒
> 2. Google Chrome 瀏覽器是否就緒
> 3. TWSE CNN 深度學習模型是否齊全
> 4. 互動式輸入 Google Drive / Telegram / Email 參數並自動生成 `.env`（支援 Bypass 與單項微調）
> 5. 內建一鍵執行連線測試與線程切換（4 Workers 安全穩健 / 6 Workers 極速衝刺）！

---

### 方式二：手動安裝依賴與設定

```bash
# 升級 pip 並安裝專案依賴
python -m pip install --upgrade pip
pip install -r requirements.txt
```

---

## 3. 環境變數與組態設定 (.env)

若您希望在本地執行時也能自動將成果備份至 Google Drive 或發送 Telegram / Email 通知，可在專案目錄建立 `.env` 檔案：

```ini
# Google Drive 雲端同步 (推薦 GAS 模式，解決個人帳號 403 空間配額限制)
GDRIVE_UPLOAD_URL=https://script.google.com/macros/s/AKfycb.../exec
GDRIVE_FOLDER_ID=1L6f9j...h_0a

# Telegram 即時推播 (選填)
TELEGRAM_BOT_TOKEN=8289732762:AAHyI84VYLSRNbjXjHeedpnP1h5v0UkfIxg
TELEGRAM_CHAT_ID=1634376784

# SMTP 郵件報表 (選填)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=xxxx xxxx xxxx xxxx
RECEIVER_EMAIL=your_email@gmail.com
```

---

## 4. 核心執行指令與情境範例

所有採集指令皆透過核心入口程式 [`stock_crawler_coordinator.py`](file:///d:/MyProject/stock_data_downloader/stock_crawler_coordinator.py) 調度執行：

### 4.1 一鍵採集全市場（上市 + 上櫃）

```bash
# 預設抓取最近一個交易日 (極速 Parquet 模式，推薦 4 Workers)
python stock_crawler_coordinator.py --market all --workers 4 --no-excel

# 同時輸出 Excel (.xlsx) 報表
python stock_crawler_coordinator.py --market all --workers 4
```

---

### 4.2 僅採集上市股票 (TWSE)

```bash
# 上市股票支援純 HTTP 高併發 (可開 8 Workers，約 3~5 分鐘完成)
python stock_crawler_coordinator.py --market twse --workers 8 --no-excel
```

---

### 4.3 僅採集上櫃股票 (TPEX - 本地多進程加速)

```bash
# 本地端將自動路由調用 tpex_crawler_local.py (4-Workers 多進程並行)
python stock_crawler_coordinator.py --market tpex --workers 4
```

---

### 4.4 歷史特定日期採集與補抓

```bash
python stock_crawler_coordinator.py --date 2026-08-21 --market all --workers 4 --no-excel
```

---

## 5. 本地端 TPEX 專屬對策與斷點續傳機制

本地端已採用獨立專屬模組 [`tpex_crawler_local.py`](file:///d:/MyProject/stock_data_downloader/tpex_crawler_local.py)，具備以下優勢：

1. **4~8 Workers 多進程平行矩陣**：
   * 每個 Worker 分配獨立 Chromium 進程與暫存目錄，全速並行採集，耗時由 2 小時縮短至 **15~20 分鐘**。
2. **實體按鈕精準判定**：
   * 徹底移除模糊文字搜尋，以實體下載按鈕作為唯一判定，**100% 杜絕 00679B 等標的被誤判無交易**。
3. **斷點續傳快取 (Checkpoint Resume)**：
   * 抓取過程中自動在 `downloads_tpex_local/checkpoints/` 寫入進度。
   * 若執行中途手動中斷（Ctrl+C）或電腦關機，**下次啟動會自動載入已完成標的並跳過，秒級接關**！

---

## 6. 輔助工具與連線測試

### 6.1 本地驗證 Google Drive 雲端同步
```bash
python test_gdrive.py
```

### 6.2 本地測試 Telegram / Email 報表
```bash
python test_notify.py
```

### 6.3 資料完整性核對與自動修復 (verify_crawler.py)
```bash
python verify_crawler.py --date 2026-08-21 --market all --auto-repair
```

### 6.4 Parquet 高速批次轉 Excel (parquet_to_excel.py)
```bash
python parquet_to_excel.py output/api_absr1_2026-08-21_2026-08-21.parquet
```

---

## 7. 本地自動排程設置教學

### 7.1 Windows 工作排程器 (Task Scheduler)

1. 按 `Win + R` 輸入 `taskschd.msc` 開啟「工作排程器」。
2. 點選「建立基本工作」➔ 名稱填寫 `台股券商分點每日爬蟲`。
3. 觸發程序選擇「每天」，時間設定為 **每週一至週五 下午 17:30**。
4. 動作選擇「啟動程式」：
   * **程式或指令碼**：`python` (或 Python 完整路徑 `C:\Users\...\python.exe`)
   * **新增引數**：`stock_crawler_coordinator.py --market all --workers 4 --no-excel`
   * **開始於**：`d:\MyProject\stock_data_downloader`

---

### 7.2 Linux / macOS 定時排程 (Crontab)

```bash
# 編輯排程
crontab -e

# 週一至週五 17:30 自動執行
30 17 * * 1-5 cd /path/to/stock_data_downloader && python stock_crawler_coordinator.py --market all --workers 4 --no-excel >> crawler.log 2>&1
```

---

## 8. 常見問題排解 (FAQ)

### Q1: 為什麼執行 setup_wizard.py 時提示跳過 (Bypass)？
> **解答**：當偵測到 `.env` 已經有完整設定時，精靈會主動提供 Bypass 選項（按 Enter 即可直接跳過回到主選單），也可以輸入 `2`、`3`、`4` 單獨修改某一項設定。

### Q2: 00679B、00687B 真的有分點資料嗎？
> **解答**：有的。系統已徹底拔除文字模糊比對，改為直接檢查實體「下載 CSV」按鈕，實測 00679B（605 筆）、00687B（453 筆）皆 100% 正確下載並完成聚合解析。
