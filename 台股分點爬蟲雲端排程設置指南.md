# 台股券商分點爬蟲雲端排程設置指南

本文件提供將台股分點爬蟲部署至雲端環境（**GitHub Actions 零成本伺服器、Linux VPS 主機、Docker 容器**）的完整設置流程、排程範本與自動化歸檔設定。

---

## 📌 目錄
1. [方案一：GitHub Actions 零成本自動排程 (推薦)](#1-方案一github-actions-零成本自動排程-推薦)
2. [方案二：Linux VPS / NAS 伺服器排程部署](#2-方案二linux-vps--nas-伺服器排程部署)
3. [雲端儲存與自動同步 (Google Drive / HuggingFace / PostgreSQL)](#3-雲端儲存與自動同步)
4. [即時通知警報 (Discord / Telegram 推播)](#4-即時通知警報)

---

## 🚀 1. 方案一：GitHub Actions 零成本自動排程 (推薦)

GitHub Actions 提供每週一至週五盤後定時觸發，並自動將產出的 Parquet 與 Excel 打包上傳供下載，完全無需自備伺服器。

### (1) 建立工作流程檔案
在專案根目錄建立 `.github/workflows/daily_stock_crawler.yml`：

```yaml
name: 每日全市場券商分點爬蟲排程

on:
  schedule:
    # 每週一至週五 台灣時間 20:30 (UTC 12:30)
    - cron: '30 12 * * 1-5'
  workflow_dispatch: # 支援在 GitHub 網頁手動一鍵觸發
    inputs:
      target_date:
        description: '指定抓取日期 (YYYY-MM-DD，留空則自動判斷最新交易日)'
        required: false
        default: ''
      market:
        description: '抓取市場 (all / twse / tpex)'
        required: true
        default: 'all'

jobs:
  run-crawler:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: 檢出專案代碼 (Checkout)
        uses: actions/checkout@v4

      - name: 設定 Python 環境
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: 安裝系統依賴 (Chromium for TPEX)
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser xvfb

      - name: 安裝 Python 套件依賴
        run: |
          pip install --upgrade pip
          pip install -r stock_data_downloader/requirements.txt
          pip install tensorflow

      - name: 執行全市場分點爬蟲 (僅產出 Parquet)
        env:
          PYTHONIOENCODING: "utf-8"
        run: |
          TARGET_DATE="${{ github.event.inputs.target_date }}"
          MARKET="${{ github.event.inputs.market }}"
          
          if [ -z "$MARKET" ]; then
            MARKET="all"
          fi
          
          if [ -n "$TARGET_DATE" ]; then
            python stock_data_downloader/stock_crawler_coordinator.py --market "$MARKET" --date "$TARGET_DATE" --workers 2 --no-excel
          else
            python stock_data_downloader/stock_crawler_coordinator.py --market "$MARKET" --workers 2 --no-excel
          fi

      - name: 上傳成果為 GitHub Artifact (僅 Parquet，保留 7 天)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: stock-bsr-parquet-${{ github.run_number }}
          path: stock_data_downloader/output/*.parquet
          retention-days: 7
```

---

## 🖥️ 2. 方案二：Linux VPS / NAS 伺服器排程部署

適用於 GCP、AWS EC2、DigitalOcean、自建 Ubuntu 伺服器或 Synology NAS。

### (1) 主機環境初始化
```bash
# 1. 更新套件庫與安裝基本工具
sudo apt update && sudo apt install -y python3 python3-pip python3-venv git chromium-browser

# 2. 複製專案並建立虛擬環境
git clone https://github.com/your-username/MyProject.git /opt/stock_crawler
cd /opt/stock_crawler
python3 -m venv venv
source venv/bin/activate

# 3. 安裝必要套件
pip install -r requirements.txt
pip install tensorflow DrissionPage opencv-python
```

### (2) 設定 Crontab 定時排程
輸入 `crontab -e` 並在最下方新增：
```bash
# 每週一至週五 晚上 20:30 (台灣時間) 自動執行爬蟲並記錄 log
30 20 * * 1-5 cd /opt/stock_crawler && /opt/stock_crawler/venv/bin/python dataProject/stock_crawler_coordinator.py --market all --workers 2 >> /var/log/stock_crawler.log 2>&1
```

---

## ☁️ 3. 雲端儲存與自動同步

為了避免產出的 Parquet/Excel 檔案佔滿主機空間，可選擇自動上傳至雲端儲存：

### 方式 A：自動上傳 Hugging Face Datasets (免費、版本化管理)
1. 在 GitHub Secrets 設定 `HF_TOKEN`。
2. 爬蟲執行結束後執行一鍵同步腳本：
```python
from huggingface_hub import HfApi
api = HfApi(token="YOUR_HF_TOKEN")
api.upload_folder(
    folder_path="dataProject/output",
    repo_id="your-username/tw-stock-broker-data",
    repo_type="dataset"
)
```

### 方式 B：自動上傳 Google Drive (rclone 工具)
```bash
# 透過 rclone 自動將產出同步至 Google 雲端硬碟
rclone sync dataProject/output/ gdrive:StockData/
```

---

## 🔔 4. 即時通知警報 (Discord / Telegram)

在 `stock_crawler_coordinator.py` 結尾加入 Webhook 推播，執行完畢後立即將今日結果與總筆數通知至手機：

```python
import requests

def send_discord_notify(webhook_url, date_str, total_symbols, total_rows):
    payload = {
        "content": f"✅ **【台股分點資料抓取完成】**\n- 交易日期: `{date_str}`\n- 涵蓋標的: `{total_symbols}` 檔\n- 總分點筆數: `{total_rows:,}` 列\n- 檔案已產出至 output 目錄 (Parquet / Excel)。"
    }
    requests.post(webhook_url, json=payload, timeout=10)
```

---

## 🎯 總結與最佳實踐建議

1. **首選方案**：建立 `.github/workflows/daily_stock_crawler.yml`，使用 GitHub Actions 免費排程，每日盤後自動抓取並儲存於 Artifacts。
2. **防頻率限制原則**：雲端執行時 Worker 數建議維持 **2 Workers**，保持適當延遲以維持 99% 的高成功率。
