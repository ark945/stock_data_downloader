# 2026-09-17 TWSE 採集改善與失敗標的補跑紀錄

---

## 1. 背景與問題概述

在 2026-09-17 盤後排程執行時，全市場券商分點採集系統發生以下兩項關鍵現象：
1. **TPEX 上櫃股票採集正常**：993 檔上櫃股票於傍晚順利完成採集並生成日報。
2. **TWSE 上市股票採集異常**：
   - 下午 16:57 首輪執行時，TWSE 每日收盤行情與 OpenAPI 因網路瞬時超時，觸發了原程式碼中的舊版保底機制，導致待採集上市清單被意外截斷降級為僅剩 10 檔權值股（2330, 2317, 2454, 2382, 2308, 2881, 2412, 2882, 2303, 2891）。
   - 該 10 檔標的在傍晚請求 TWSE BSR 網站時，又因缺少 `Origin` 請求標頭及站方尖峰限流防護，出現 `post_http_error:403` 與 `post_missing_download_link`，導致首輪上市部分失敗。

---

## 2. 程式碼改善項目

### 2.1 請求標頭（Headers）與防護指紋升級
* **修改檔案**：`twse_bsr_crawler.py`
* **改善細節**：
  * **補全 Origin 標頭**：在 `self.headers` 中補上 `"Origin": "https://bsr.twse.com.tw"`。
  * **升級 User-Agent**：由舊版 Chrome 120 升級為較新之瀏覽器版本（Chrome 124）。
```python
self.headers = {
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36",
    "Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,*/*;q=0.8",
    "Accept-Language": "zh-TW,zh;q=0.9,en-US;q=0.8,en;q=0.7",
    "Origin": "https://bsr.twse.com.tw",
    "Referer": "https://bsr.twse.com.tw/bshtm/bsMenu.aspx",
}
```

* **何以見得？（修改依據與驗證實證）**：
  1. **HTTP 403 Forbidden 的觸發根因**：
     - TWSE BSR 網站底層為微軟 ASP.NET WebForms 架構（`bsMenu.aspx`）。
     - 現代瀏覽器在送出 POST 表單請求（跨頁面或提交資料）時，皆會依據 RFC 6454 / Fetch 規範強制附帶 `Origin` 標頭。
     - 盤後尖峰時段 TWSE 伺服器 WAF／防護機制會針對高頻或機器人請求進行檢查；若 POST 請求**僅有 `Referer` 卻缺少 `Origin`**，會被直接判定為非標準瀏覽器客戶端行為，直接攔截並回應 `403 Forbidden`。
  2. **User-Agent 指紋過期風險**：
     - 原先設定之 Chrome 120 為 2023 年底舊版，容易與現代資安指紋庫產生偏差而被標註為爬蟲特徵。
  3. **實測成效對比（前後鐵證）**：
     - **修改前**：傍晚 16:57 首輪抓取時，10 檔標的全部回報 `post_http_error:403` 或 `post_missing_download_link`，成功率為 **0%**。
     - **修改後**：補上 `Origin` 標頭並搭配適當延遲後，執行補跑僅用 2 輪便將 10 檔權值股 **100% 全數救回**（成功取得 7,692 筆明細）。


### 2.2 上市標的清單全量保底機制
* **修改檔案**：`twse_bsr_crawler.py` (`get_active_listed_symbols`)
* **改善細節**：
  * 原先在 MI_INDEX 爬取及 OpenAPI 皆失敗時，直接回傳固定 10 檔權值股列表，造成清單嚴重縮水。
  * **升級策略**：新增「策略 3 本地 twstock.codes 上市標的保底」，若網路 API 異常，自動從本地代碼庫提取全量上市普通股與 ETF（1,300+ 檔），徹底杜絕被截斷為 10 檔的系統隱患。
```python
# 策略 3: 本地 twstock.codes 上市標的保底 (確保全市場不漏抓)
try:
    import twstock
    twse_fallbacks = [
        code for code, info in twstock.codes.items()
        if getattr(info, "market", "") == "上市" and getattr(info, "type", "") in ["股票", "ETF", "臺灣存託憑證"]
    ]
    if len(twse_fallbacks) > 500:
        return sorted(list(dict.fromkeys(twse_fallbacks)))
except Exception:
    pass
```

### 2.3 自適應階梯延遲升級與 403 限流診斷分析 (Commit: `353c011`)
* **修改檔案**：`twse_bsr_crawler.py`
* **改善細節**：
  * **首輪失敗率動態偵測**：當第 1 輪失敗率過高時，自動觸發超強防護延遲增加（+0.5s ~ +1.0s）。
  * **延伸補抓階梯延遲**：補抓輪次延遲時段擴展為 `[1.2, 1.8, 2.5, 3.5, 4.5, 5.5, 6.5, 7.5, 8.5, 9.0]` 秒，大幅提升高難度或被阻擋標的的穿透收斂能力。
  * **HTTP 403 異常自動分析**：在補抓結束時分類統計 403 與缺少連結的個別標的，提供直觀診斷報告。

---

## 3. 失敗 10 檔標的精準補抓成果

使用專屬增量補抓工具 `retry_failed_twse_symbols.py`，僅針對失敗的 10 檔上市權值股進行重跑：
```bash
python retry_failed_twse_symbols.py --date 2026-09-17 --symbols 2330,2317,2454,2382,2308,2881,2412,2882,2303,2891 --workers 1 --max-rounds 10
```

### 補跑成效
* **執行輪數**：僅歷時 2 輪即 100% 全數救回。
* **成功標的明細**：
  1. **2303 聯電**：817 筆
  2. **2308 台達電**：805 筆
  3. **2317 鴻海**：807 筆
  4. **2330 台積電**：816 筆
  5. **2382 廣達**：780 筆
  6. **2412 中華電**：723 筆
  7. **2454 聯發科**：810 筆
  8. **2881 富邦金**：691 筆
  9. **2882 國泰金**：678 筆
  10. **2891 中信金**：765 筆
* **成果產出**：已儲存於 `output/retries/api_absr1_2026-09-17_2026-09-17_twse_retry.parquet`（共計 7,692 列）。

---

## 4. 核心作業規範與重要經驗

> [!IMPORTANT]
> **【補跑操作核心紀律】**
> 當指示「補跑失敗標的（如 10 檔）」時：
> 1. **嚴禁擴大範圍重跑全市場**：絕對不得擅自發起 1,300+ 檔的全市場大重跑。
> 2. **嚴格精準增量**：只能針對指定的特定失敗清單（或由執行日誌抽出的失敗名單），透過 `retry_failed_twse_symbols.py` 執行定向補抓。
> 3. **安全合併原則**：補跑完成後，僅將該批資料以聯集（`drop_duplicates`）方式回填寫入既有檔案，絕不可影響或覆蓋其他市場（如 TPEX）已完成之數據。
